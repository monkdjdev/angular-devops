apiVersion: v1
kind: Service
metadata:
  name: {{ include "angular-devops.fullname" . }}
  labels:
    {{- include "angular-devops.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "angular-devops.selectorLabels" . | nindent 4 }}
